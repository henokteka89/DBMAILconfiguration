--Things not to forget
---to enable log-in (authentication), you need a password generated by Gmail - go to 2-step verification and generate an "APP" password (shown at bottom corner)
--Gmail SMTP (smtp.gmail.com) port number 587 
--Mark SSL

to check new or existing profile and operator details 
SELECT * FROM msdb.dbo.sysmail_profile;
SELECT * FROM msdb.dbo.sysmail_account;
GO
